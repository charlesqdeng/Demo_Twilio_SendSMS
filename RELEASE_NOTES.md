# Release Notes

## v1.0.0 - Initial release

- Initial public release of the Twilio SMS Bulk Messaging Application.
- Features: Excel-based recipients, Twilio Lookup validation, template support, dry-run mode, and error logging.

To create a GitHub release, a tag `v1.0.0` is created and pushed to the remote.
